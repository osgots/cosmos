/**
 * Stable identifier for anything represented inside COSMOS Infinity.
 *
 * The exact generation algorithm is deliberately NOT defined here.
 * That belongs to the ontology/procedural systems later.
 */
export type UniverseEntityId = string;
