/**
 * Core mathematical constants used throughout COSMOS Infinity.
 *
 * JavaScript numbers are IEEE-754 double-precision approximations.
 * Irrational constants such as pi and e cannot be represented exactly
 * by any finite decimal or binary floating-point value.
 */

export const PI = Math.PI;

export const TAU = 2 * Math.PI;

export const EULER_NUMBER = Math.E;

export const SQRT_TWO = Math.SQRT2;

export const GOLDEN_RATIO = (1 + Math.sqrt(5)) / 2;
